<p id="notice"><%= notice %></p>

<div id="itemsContainer">

<h1><%= @item.title %></h1>

<p>
  <strong>Author:</strong>
  <%= @item.author %>
</p>

<%= image_tag @item.image_url, :class => 'welcomlm' %>

<p>
  <%= @item.synopsis %>
</p>

  <div class="itemhols">
  <p>Price <%= number_to_currency(@item.price, :unit => "€") %>
  <br>
  Category: <%= @item.category %></p>

  <p>
  <%= link_to 'Buy', cart_path, :class => "button", :role => "button" %>
  <%= link_to 'Rent', cart_path, :class => "button", :role => "button" %>
  <%= link_to 'Back', items_path, :class => "button", :role => "button" %>
  <% if session[:login] == 1 %>
  <%= link_to 'Edit', edit_item_path(@item), :class => "button", :role => "button" %>
  <% end %>
  </p>

  </div>
</div>